/** Minimal ESLint flat config for Next.js 15 (extend when eslint is installed). */
const eslintConfig = [
  {
    ignores: [".next/**", "node_modules/**", "out/**"],
  },
];

export default eslintConfig;
